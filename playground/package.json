{
  "private": true,
  "scripts": {
    "dev": "nodemon -w '../src/**/*.ts' -e .ts -x vite --host",
    "build": "vite build"
  },
  "devDependencies": {
    "vite": "4.0.4",
    "vite-plugin-inspect": "^0.7.12"
  }
}
